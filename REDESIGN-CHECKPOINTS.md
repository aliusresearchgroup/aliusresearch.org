# ALIUS Redesign Checkpoints

This log catalogs pushed redesign checkpoints so individual features can be
reverted selectively if they do not work for the organization.

| Checkpoint | Commit | Scope | Verification | Revert Notes |
|---|---|---|---|---|
| checkpoint-001 | this commit | Installed Pretext skills; created top-level page folders; wired shared assets; added sticky seven-tab nav, left/page anchors, Team data extraction, obfuscated email PNGs, and Pretext-enhanced Team expansion. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/tablet/mobile for all seven routes plus Team expand/email/Escape checks. | Revert this checkpoint to remove the folder migration, shared redesign layer, and first Team-grid interaction pass. |
| checkpoint-002 | this commit | Rebuilt Events as one compact page: ASSC Satellite overview, 2x2 theme grid, chronological speaker timeline, Pretext-expanded speaker biographies, QRI partner details, and consolidated previous ALIUS event segments. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/tablet/mobile for Events, including one section nav, no horizontal overflow, no console errors, click expansion, and Escape collapse. | Revert this checkpoint to restore the pre-compact Events page while keeping the broader redesign shell from checkpoint-001. |
| checkpoint-003 | this commit | Cleaned Bulletin typography: replaced broken double-encoded issue labels with `No.` labels, applied Bulletin-specific scholarly heading/body font rules, and tightened legacy divider spacing. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/tablet/mobile for Bulletin, including computed EB Garamond/Lora fonts, no mojibake, no horizontal overflow, no console errors, and anchor click/active-state behavior. | Revert this checkpoint to restore the previous Bulletin typography while keeping the Events and Team redesign checkpoints. |
| checkpoint-004 | this commit | Pinned the shared left-side page anchor menu to the far-left edge of the viewport on desktop while preserving the compact bottom anchor rail on tablet/mobile. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/tablet/mobile for Home, About, Team, Bulletin, Journal Clubs (`/journal-club/`), Events, and Membership with desktop anchor menu `x = 0`, no horizontal overflow, and no console errors. | Revert this checkpoint to restore the previous content-container-aligned anchor menu. |
| checkpoint-005 | this commit | Refined Team steady cards so profile icons stay visible without row overlap; restored compact animated ASSC theme visuals; added Bulletin PDF embeds at the bottom of each issue segment; tightened A4-like prose measure and sidebar spacing. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/mobile for Team, Events, and Bulletin with no horizontal overflow, no sidebar overlap, Team expand/Escape checks, four animated theme visuals, and `14` Bulletin PDF embeds. | Revert this checkpoint to remove the Team card spacing pass, Events theme micro-animations, Bulletin PDF embed blocks, and related reading/sidebar CSS refinements. |
| checkpoint-006 | this commit | Combined the Home mission text into the top of About, replaced the old About opening with a clean reading-first intro, added meaningful About anchors, tightened the About essay measure, and changed tablet/mobile anchor navigation so it no longer floats over prose. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed for About at desktop/tablet/mobile with no horizontal overflow, no console errors, no anchor/prose overlap, desktop anchor menu `x = 0`, and mobile References anchor landing below the sticky rail. A shared anchor-nav regression pass also covered all seven routes at desktop/mobile. | Revert this checkpoint to restore the previous About opening, previous About typography, and the former fixed bottom mobile anchor rail. |
| checkpoint-007 | this commit | Replaced the first three ASSC Four Themes mini-animations with compact restorations of the original archived motifs: Track 1 thermometers plus torus, Track 2 asymmetric VR headset with animated screen kernel, and Track 3 lived-experience wave/profile visual; preserved the current Formal State Spaces visual. Also tightened mobile anchor positioning so the section rail does not overlap the main nav or anchor headings. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed at desktop/mobile for Events with no horizontal overflow, no console errors, active animations for all four theme visuals, restored theme markers present, and mobile anchor-click checks showing no overlap with the main nav or `Four Themes` heading. | Revert this checkpoint to restore the prior simplified Events theme icons and the previous mobile anchor-scroll offsets. |
| checkpoint-008 | this commit | Added the About essay underneath the Home hero; compacted the ASSC Satellite logistics into one facts card; changed the ASSC program into day-based side-by-side session groups; promoted every past event into its own Events page segment and left-nav anchor; simplified Membership into a single readable page without accordions or dropdowns; and reinforced mobile anchor spacing. | Build passed; link check passed (`278` HTML files, `0` missing local links/assets); Playwright visual QA passed for Home, Events, and Membership at desktop/mobile with no console errors, no horizontal overflow, solid sticky navigation backgrounds, readable mobile anchor landings, and Events day-program/card layout checks. | Revert this checkpoint to restore the prior Home/About separation, exact-time Events timeline, previous-events grid, and old Membership accordion page while preserving earlier redesign checkpoints. |
